import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mattypattysapps.crystalcrush',
  appName: 'Crystal Crush',
  webDir: 'dist',
  backgroundColor: '#020617',
};

export default config;
